import {StyleSheet} from 'react-native';

export default styles = StyleSheet.create({
  timePeriodButton: {
    width: '12%',
    justifyContent: 'center',
    alignItems: 'center',
    paddingVertical: 10,
  },
});
